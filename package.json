{
  "name": "olympus-dashboard-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only HermesOS Agent Monitor dashboard plugin.",
  "scripts": {
    "dev": "scripts/install-dashboard-link.sh && hermes dashboard --no-open --skip-build",
    "verify": "python3 -m py_compile dashboard/plugin_api.py && python3 -m unittest tests.test_privacy_contract tests.test_trace_spine tests.test_skill_hygiene && node --check dashboard/dist/index.js && git diff --check",
    "test:visual": "playwright test",
    "test:visual:artifacts": "playwright test",
    "test:live": "node scripts/olympus-live-smoke.mjs",
    "test:security": "node scripts/olympus-security-smoke.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
